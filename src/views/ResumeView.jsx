import styled from "styled-components";

const Wrapper = styled.div`
  color: var(--primary-color-900);
`;

const ResumeView = () => {
  return <Wrapper>hello resume</Wrapper>;
};

export default ResumeView;
